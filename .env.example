# Copy to .env.local. NEVER commit .env.local.
# Key is used server-side only (API route). It must never reach the client bundle.
OPENAI_API_KEY=sk-proj-xxxxxxxxxxxxxxxxxxxx

# Optional: cap spend during eval runs
MAX_STEPS=12
MAX_TOOL_RETRIES=1
